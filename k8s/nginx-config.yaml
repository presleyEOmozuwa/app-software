apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  default.conf: |
    server {
      listen 80;

      location / {
        proxy_pass http://react-frontend:5173;
      }

      location /api {
        proxy_pass http://node-backend:3000;
      }
    }
